# 登录契约后置计划

## 后置说明

账号身份映射和重复登录一致性已经通过实际链路验证，包括 LayaIDE guest 登录和微信小游戏自动登录。Central 网关分配强转 bug 已修复，交替登录不再失败。用户已决定将微信可选资料授权押后。

## 后置工作

- [ ] 确定账号登录后的非阻断资料授权入口，接入微信原生用户信息按钮，并在微信开发者工具分别验证已有权限、拒绝授权和权限失效场景：已验证资料能够更新展示信息，拒绝或资料 API 失败仍不改变登录状态。

## 阻塞

- 当前会话中出现过明文 AppSecret。真实联调前必须先在微信后台重置，并只通过 WECHAT_APP_SECRET 环境变量注入新值。

## 完成条件

- 已授权资料由服务端验证后更新；授权拒绝、权限失效或资料 API 失败均不阻断账号登录。
- 仓库、日志、构建产物和文档均不含 AppSecret、原始 code、session_key 或真实 fixture 凭据。