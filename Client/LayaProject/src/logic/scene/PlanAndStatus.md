# Scene 当前计划

## 当前目标

Scene 时间调度及 gameplay 时间单位已经收口，可重复调度验证、BattleScene 暂停/倍速和三次场景缓存恢复均已通过。当前只剩真实可见性切换环境下的后台恢复验证。

## 待完成

- [ ] 在能够可靠触发页面/应用进入后台的浏览器或目标设备环境验证后台恢复：Realtime 补入后台经过时间并到达最新状态；FixedTick 忽略后台经过时间并从原 tick 恢复。

## 阻塞

- 当前 LayaAir IDE 预览环境无法进入真实后台状态，不能触发有效的可见性切换；该项等待浏览器预览或目标设备环境。

## 验收条件

- 下层 gameplay 不读取时间模式或墙钟，不建立绕过 Scene 的独立 Timer。
- FixedTick 默认 30Hz，单 Laya 帧最多追 5 次；超过 3 次时跳过渲染且下次累计推进表现。
- Realtime 不截断大 dt；两种模式使用相同的下层 `curTime` 比较逻辑。
- TypeScript、文档检查通过；后台策略需在能触发真实可见性切换的环境完成实测。
