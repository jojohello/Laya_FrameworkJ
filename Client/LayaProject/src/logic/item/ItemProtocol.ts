/**
 * Logic-side protocol placeholder.
 *
 * The real network Protocol base lives in the Start package. Item/Bag server
 * sync will be connected later without making Logic depend on Start directly.
 */
export class ItemProtocol {
    register(): void {
    }

    release(): void {
    }
}
