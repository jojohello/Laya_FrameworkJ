# AI 文档接入协议

本文件只规定智能体如何读取和维护项目文档，不承载具体模块设计。项目事实以对应作用域内的 `README.md`、`DESIGN.md` 和 `PlanAndStatus.md` 为准。

## 开始任务

1. 确定改动的最小目录作用域。
2. 从仓库根目录开始，按父目录到目标目录的顺序读取沿途存在的 `DESIGN.md`。
3. 读取目标目录及必要父目录的 `README.md`，确认当前能力、入口和用法。
4. 只有任务涉及未完成工作、优先级或交接时，才读取相关 `PlanAndStatus.md`。
5. 对照实际代码、配置和生成脚本验证文档结论。

涉及 `Client`、`Sever`、`Protocol` 中两个或以上作用域，或涉及服务端权威状态、请求/响应、Push、登录后同步、重连恢复、奖励、背包、货币、结算、身份和精确整数的任务，必须使用仓库根级 `$laya-client-server-feature` Skill。无论从哪个子目录启动，都以同一跨端契约和 Gate 顺序交付，不得先让两端各自形成独立协议。

文档不存在是合法状态。不要为了补齐形式创建文件，也不要依赖某个智能体的私有记忆代替仓库文档。

## 文档边界

| 文件 | 内容 |
| --- | --- |
| `README.md` | 当前能力、公开入口、命令、目录索引和使用示例 |
| `DESIGN.md` | 稳定架构、作用域边界、约束、取舍和重复错误防范 |
| `PlanAndStatus.md` | 当前未完成任务、顺序、阻塞和验收条件 |

模块信息写入最近的有效作用域；跨模块契约写入最近的共同父目录。

## 更新规则

- 行为或公开入口变化时更新最近的 `README.md`。
- 稳定设计或约束变化时更新最小有效作用域的 `DESIGN.md`。
- 当前任务、顺序或阻塞变化时更新相应 `PlanAndStatus.md`。
- 任务完成后，先提炼长期有效信息，再删除完成项；Plan 没有未完成内容时直接删除。
- 内层规则可以补充外层规则。违反外层规则必须在内层 DESIGN 声明例外、原因和影响，并得到用户确认。
- 新规则应来自实际需求、重复错误或已确认决策；及时移除失效规则。

不要保留进度日记、完成清单和日期式交接记录，追溯由 Git 历史承担。

## 文本与生成物

项目自维护源码和文档统一使用 UTF-8 无 BOM、LF。引擎或工具生成文件按对应作用域规则处理，不为了格式单独重写生成物。

修改配置或协议源文件后，必须运行对应生成器并同时提交源文件与纳入版本管理的生成结果。禁止手改生成文件来绕过源数据。

## 验证

从仓库根目录运行：

```powershell
powershell -ExecutionPolicy Bypass -File tools/docs/validate-doc-system.ps1
```

具体构建和静态检查命令见各子项目 `AGENTS.md` 或 `README.md`。
